# classroom_v4_3.html 帧数优化计划

> 目标：在不降低画质、**不使用任何"运动中模糊/降分辨率"补帧手段**的前提下提升帧率。
> 测量环境：Apple M4 / ANGLE Metal / Chromium，渲染目标 1685×948。

---

## 第二轮（2026-09-24）：从 21 FPS 到 90–161 FPS

### 怎么定位的

浏览器面板会把 `requestAnimationFrame` 节流到 ~1Hz，直接读 FPS 没有意义。
改用 `gl.readPixels` 强制管线同步，逐项计时单帧真实成本：

| 配置 | ms/帧 | FPS |
|---|---|---|
| 原始（场景内 39 盏灯） | 44.6 | 22 |
| 只留 4 盏（环境/半球/太阳/月亮） | **3.6** | 278 |
| 隐藏一半网格（灯不动） | 37.1 | 27 |
| composer 分辨率 1.75→1.0（灯不动） | 18.7 | 53 |
| 关 GTAO | −0.5 ~ −3 | |
| 4096² 阴影贴图重绘一次 | +2.3 | |
| drawBloom / drawGrain 叠层 | ~0.01 | |

结论很干脆：**帧时间的约 93% 花在"每个 fragment 把 39 盏灯全遍历一遍"的片元着色上**，
跟网格数（1109）、draw call（982）、阴影贴图、后处理几乎无关。
降分辨率之所以能提帧，正因为瓶颈在片元；而降分辨率恰恰是本项目不许用的手段。

还有一条关键观察：代价是**超线性**的。点光源从 21 盏降到 12 盏，帧时间从 46ms 掉到 20ms
（每盏约 2.9ms）；从 6 盏降到 0 盏只省 1.6ms（每盏约 0.27ms）。灯多到一定程度后，
着色器体积撑爆 GPU 寄存器、占用率崩塌，所以**减灯的收益远大于线性预期**。

### 做了什么

按"改动 → 省多少 → 画面差多少（meanAbs，满量程 255）"排列：

| 改动 | 省下 | 画面差异 | 说明 |
|---|---|---|---|
| **SpotLight 只算漫反射** | 44.6→34.6ms | **0.19** | 10 盏 SpotLight 全是 `castShadow=false` 的**间接光近似**（窗内散射 / 地面→吊顶反弹 / 地砖→桌底反弹 / 走廊漏光）。它们模拟的是漫反射 GI，本来就不该产生 GGX 镜面高光。见 `makeSpotLightsDiffuseOnly` |
| **走廊 8 盏吸顶灯按相机位置整组开关** | ~6ms | 0.11 | 人在教室里时它们的贡献只有 0.11/255（走廊是靠自发光被看见的）；进了走廊才点亮。见 `LAYER_CORRIDOR` |
| **教室 7 盏反弹补光按相机位置整组开关** | ~8ms（走廊视角） | ≤0.23 | 反过来：人在走廊时，教室窗侧那 7 盏反弹补光看不见。见 `LAYER_ROOMFILL` |
| **2 盏门玻璃面光源 → 漫反射聚光** | ~3ms | 0.43 | RectAreaLight 走 LTC 积分，是最贵的灯型（4 盏合计约 8.3ms）。门玻璃只有 0.8×0.98m，2~3m 外与大张角柔边聚光无法区分 |
| **3 盏地面反弹补光合并成 1 盏** | ~2ms | ≤0.45 | 三个光域沿 z 完全重叠成一条带，张角加宽 + 光通量×3 补回 |
| **删掉恒为 0 的 corridorLight** | ~2ms | 0 | 渲染循环里无条件 `intensity = 0`，却仍占着一份 uniform 槽位和一整段展开循环 |

**合计：44.6ms → 9.4ms（约 4.7×）**，随后把画幅 `FRAME_FILL` 从 0.88 提到 0.94
（像素数 +14%，画面明显更大），最终稳定在 **90–161 FPS**。

### 试过但否决的

| 方案 | 为什么不用 |
|---|---|
| PointLight 也去掉高光 | 再省 10ms，但 meanAbs=3.27 —— 地面/桌面丢失吊顶灯具的反射，质感明显变平 |
| 用 Hammon 近似替换 `V_GGX_SmithCorrelated`（去 sqrt） | 实测**零收益**（45.8 vs 46.6ms）。瓶颈是整段展开循环的寄存器压力，不是某几条指令 |
| 吊顶灯 12 盏 → 6 盏 | meanAbs=10.59，地面光斑与灯位对不上，肉眼明显 |
| 走廊灯 8 盏 → 4 盏（加倍亮度） | 从教室里看 meanAbs 仅 0.23，**但站在走廊里整体暗 12% 并出现暗带**（扫了 60 组"位置×距离×亮度"组合，最优解仍有 meanAbs=13.5）。改用按相机位置整组开关 |
| ceilBounce 3 盏 → 2 盏 | meanAbs=0.66，吊顶明暗渐变可见变化 |
| 按强度动态隐藏灯（intensity≈0 就 `visible=false`） | 灯数一变就要重编全部着色器：**实测首次 1893ms 硬卡顿**。宁可少省几毫秒，也要保证任何时刻零卡顿 |

### 关于"灯数变化 = 着色器重编"

three.js 的着色器程序按灯数等参数做缓存键，灯数一变就得重新编译整套程序：
实测首次 **446~1893ms**，之后命中缓存只要 13~30ms。

所以按相机位置切换图层的方案必须配一次**预热**：开场封面还盖在画面上的时候
（`warmUpShaderVariants`，跑在 `requestIdleCallback` 里），把"在教室 / 在走廊"两种灯数组合
各 `renderer.compile` + 真实渲染一帧。之后跨越右墙实测只有 **13~16ms**，一帧之内完成，看不出来。

⚠️ 两个图层必须**永远反向同时切换**（`applyCorridorLayer` 统一处理），
否则会冒出"两层全开"这种第三种灯数组合 —— 预热没覆盖到，就会在切换瞬间卡住。

---

## 同时修掉的渲染缺陷

- **离屏渲染目标是小数尺寸。** `EffectComposer.setSize` 内部是 `renderTarget.setSize(w*pixelRatio, …)`，不取整；而 `renderer.setSize` 走 `Math.floor`。1.75 倍率下两边分别是 1576.75×887.25 和 1576×887 —— 纹理按小数分配（驱动向下取整）、viewport 却按小数设，OutputPass 铺到 canvas 时差了不到一个像素：整画面轻微发虚，转动时高频边缘（桌沿/窗框/砖缝）持续抖动。现在统一用整数设备像素驱动（`setComposerPixelSize`，取整方式与 renderer 一致用 floor），默认档位下离屏与 canvas 尺寸完全相同 → 1:1 拷贝。
- **GTAO 分辨率随清晰度档位漂移。** 原本按 CSS 像素 ×0.5 算，而场景实际渲染在 CSS×curRes 的离屏上 → 档位越高 AO 相对越糊（真实比例从 0.50 掉到 0.29），正好反了。现在跟 `curRes` 走，任何档位下比例恒为 `GTAO_SCALE`。
- **`renderer.useLegacyLights = false`**：r155 起就是默认值，r164 标为 deprecated，显式赋值不改变行为、只在控制台反复打印迁移警告。已删除。

---

## 明确不采用的手段

- **动态分辨率（转动时降分辨率）** —— 原设置面板里的「动态分辨率 · 转动时降」开关**已删除**。
  它靠拖动视角时临时降低渲染分辨率换帧率，代价是画面（以及桌椅/窗框边缘）糊一下再变清晰，
  属于用模糊补帧。现在分辨率全程恒为用户选的清晰度档位，转动中画面不变糊。
- 运动模糊 / TAA 累积 / 帧生成 —— 同理，一律不用。

---

## 第一轮（2026-07-06，已完成）

- [x] **阴影"按需刷新"在漂移模式下失效**：脏检查用精确 `equals()`，而漂移每帧都让太阳位置产生浮点级变化 → 阴影其实每帧全量重绘。改为阈值判脏（`SHADOW_DIRTY_EPS = 0.02`，对应朝向变化 <0.01°、阴影位移 <3mm）。另处理了 `_lastSunP/_lastSunT` 的 NaN 初值哨兵（`distanceTo(NaN)` 恒为 NaN，`NaN > EPS` 恒 false，会导致阴影永不更新）。
- [x] **animate 循环内逐帧 `getElementById`**：缓存元素引用。第二轮又补上了时间轴/时钟/天花板灯这几处遗漏（`elTimeSlider` / `elTimeLabel` / `elTimeDisp` / `elCeilOn` / `elCeilInt`）。
- [x] **`antialias:true` 是纯浪费**：最终画面由 OutputPass 把离屏贴图铺满 canvas，canvas 原生 MSAA 对场景内部边缘没有贡献。已改 `false`。`preserveDrawingBuffer` 保留（截图功能要回读 canvas 像素）。

---

## 复测方法

浏览器面板会节流 rAF，别直接看 FPS 读数。用强制同步计时：

```js
const gl = DBG.renderer.getContext(), px = new Uint8Array(4);
const sync = () => gl.readPixels(0, 0, 1, 1, gl.RGBA, gl.UNSIGNED_BYTE, px);
const bench = (n, body) => {
  for (let i = 0; i < 3; i++) body();      // 预热：首帧含着色器编译
  sync();
  const t0 = performance.now();
  for (let i = 0; i < n; i++) body();
  sync();
  return (performance.now() - t0) / n;     // ms/帧
};
bench(15, () => composer.render());
```

改灯数之后必须多渲染两帧再计时，否则把着色器编译时间算进去了。
画面差异用 `canvas.toDataURL` 前后对比逐像素求 meanAbs —— 场景是确定性的，噪声底为 0。
