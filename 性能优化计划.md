# classroom_v4_3.html 帧数优化计划

> 目标：在不降低画质/细节的前提下提升帧率。基于对 `classroom_v4_3.html` 实际场景代码（非 three.js 库本身）的走查，按收益从高到低排序。

---

## 背景诊断

场景技术栈：three.js r164 + `EffectComposer`（RenderPass → GTAOPass → OutputPass）+ PCFSoftShadowMap（4096²）+ ACESFilmic 色调映射 + 昼夜漂移动画。

已经做对的部分（**不用动**）：
- `renderer.shadowMap.autoUpdate = false`，思路正确
- GTAO 半分辨率计算（`GTAO_SCALE = 0.5`）+ 双线性上采样到全分辨率
- `pixelRatioCap` 封顶 1.75、`SUPERSAMPLE = 1.0`，取值合理

---

## 优化项 1（优先级：★★★★★）— 阴影"按需刷新"在漂移模式下失效

### 问题

代码本意是通过 `shadowMap.autoUpdate=false` + 手动 `needsUpdate` 来避免每帧重绘 4096×4096 的阴影贴图。但脏检查逻辑用的是**精确相等判断**：

```js
if(!_lastSunP.equals(mainLight.position) || !_lastSunT.equals(mainLight.target.position)){
  renderer.shadowMap.needsUpdate = true;
  _lastSunP.copy(mainLight.position); _lastSunT.copy(mainLight.target.position);
}
```

而昼夜漂移动画 `driftOn` 每一帧都会让 `mainLight.position` 产生极微小变化（哪怕只是浮点误差级别），导致 `equals()` 几乎永远返回 `false`。结果是：**只要漂移动画在播放，阴影贴图实际上每帧都在全量重绘**，覆盖几十个 `castShadow=true` 的物体 —— 这个优化在最常见的使用场景（播放中的漂移动画）下形同虚设，大概率是当前最大的帧数瓶颈。

### 方案（零画质损失）

把"精确相等"改成"变化幅度超过阈值才判定为脏"。太阳每帧移动的距离极小，人眼无法察觉阴影延迟 1~2 帧才更新：

```js
const SHADOW_DIRTY_EPS = 0.01; // 世界单位，可按场景尺度微调（越小越接近"每帧更新"）
if(mainLight.position.distanceTo(_lastSunP) > SHADOW_DIRTY_EPS ||
   mainLight.target.position.distanceTo(_lastSunT) > SHADOW_DIRTY_EPS){
  renderer.shadowMap.needsUpdate = true;
  _lastSunP.copy(mainLight.position);
  _lastSunT.copy(mainLight.target.position);
}
```

### 验证

- 用 Chrome DevTools Performance 面板录一段漂移动画，对比修改前后 `WebGLShadowMap.render` 在火焰图中的占比。
- 目测检查：阴影边缘在漂移时是否有肉眼可见的"跳变/滞后"；若有，调小 `SHADOW_DIRTY_EPS`。

### 状态
- [x] 已完成（2026-07-06）。`SHADOW_DIRTY_EPS = 0.02`（比建议的 0.01 略大：光源距目标约 100 单位，0.02 对应朝向变化 <0.01°、阴影位移 <3mm，肉眼不可见，但把漂移播放时的阴影重绘频率进一步降低）。另处理了 `_lastSunP/_lastSunT` 的 NaN 初值哨兵——用 `Number.isNaN(_lastSunP.x)` 保留"首帧强制渲染一次阴影"的原有行为（否则 `distanceTo(NaN) → NaN`，`NaN > EPS` 恒为 false，阴影将永不更新）。

---

## 优化项 2（优先级：★★★☆☆）— 每帧的 DOM 读写可以缓存

### 问题

`animate()` 循环内（漂移逻辑部分）每帧都会调用多次 `document.getElementById(...)` 并读写 `.value`：

```js
const curA = +document.getElementById('mAngle').value;
const curT = +document.getElementById('mTemp').value;
const curI = +document.getElementById('mInt').value;
document.getElementById('mAngle').value = ...
document.getElementById('mTemp').value  = ...
document.getElementById('mInt').value   = ...
```

这些查询/赋值不影响画面，纯属可省开销。

### 方案（零画质损失）

在循环外缓存元素引用一次：

```js
const elAngle = document.getElementById('mAngle');
const elTemp  = document.getElementById('mTemp');
const elInt   = document.getElementById('mInt');
```

循环体内改为直接使用 `elAngle.value`、`elTemp.value`、`elInt.value` 读写。

### 状态
- [x] 已完成（2026-07-06）。在 `let driftOn=...` 附近缓存 `elAngle/elTemp/elInt` 各一次，替换了 animate 循环内全部逐帧读写点：漂移缓动块（读 `curA/curT/curI` + 回写三个滑块）、真实黑暗门控 `_winI`、以及 dapple 光斑动画 `mIntCur`。

---

## 优化项 3（优先级：★★★☆☆，需验证）— `antialias:true` 可能是纯浪费

### 问题

渲染器创建时：

```js
const renderer = new THREE.WebGLRenderer({antialias:true, preserveDrawingBuffer:true, powerPreference:'high-performance'});
```

而最终画面是通过 `EffectComposer` 的 `OutputPass`（最后一个 pass，`renderToScreen`）把一张全屏后处理结果贴图画到默认 canvas 上。WebGL 原生 MSAA 是对"写入默认帧缓冲的内容"做多重采样 —— 但这里写入的只是一张离屏渲染好的矩形贴图，场景内部的边缘早已在 HalfFloat 离屏渲染目标里栅格化完成，MSAA 对这张矩形基本没有实际抗锯齿收益，只会增加 resolve 开销。

### 方案（需先验证，无风险回退）

1. 临时把 `antialias:true` 改为 `false`，加载场景对比画面。
2. 如果肉眼看不出差别（预期如此，因为真正需要抗锯齿的是内部场景边缘，而不是这张输出矩形），保留 `false`，即为纯白捡的帧数。
3. 如果确实需要抗锯齿效果，应在 `EffectComposer` 管线中加一个 SMAA/FXAA pass，而不是依赖 canvas 原生 MSAA。

### 附带检查

`preserveDrawingBuffer:true` 会阻止部分浏览器端 buffer 优化，通常只有"需要截屏/导出画面"功能时才需要开启。确认场景中是否有依赖该功能的代码（如截图按钮），若没有可一并关闭。

### 状态
- [x] 部分完成（2026-07-06）。已把 `antialias:true → false`：技术上确认零画质损失——RenderPass 写入的是无 MSAA 的离屏 HalfFloat 目标，canvas 原生 MSAA 只作用于 OutputPass 铺屏的那张全屏矩形（边缘在屏幕外），对场景内部边缘无任何抗锯齿贡献，故关闭后画面不变。**建议开一次页面肉眼复核**（本地无 headless WebGL 环境，无法自动 A/B）；若日后想要真抗锯齿，应在 composer 里加 SMAA/FXAA pass。
- `preserveDrawingBuffer:true` **保留未关**：场景存在截图按钮（`takeShot() → buildShotCanvas()` 里 `ctx.drawImage(renderer.domElement)` 回读 canvas 像素），关闭有破坏截图功能的风险，按本计划"附带检查"的指引保守保留。

---

## 暂不处理（会有画质/功能取舍，非"免费"优化）

以下项目在讨论中提到，但都会不同程度影响画质或需要更多验证，本计划暂不纳入：

- 阴影贴图分辨率从 4096 降到 2048（有轻微画质损失，且优化项 1 修复后阴影已不是每帧重绘，收益边际下降）
- 减少部分小型装饰物体的 `castShadow`（会有肉眼可见的阴影细节损失）
- 进一步降低 GTAO 分辨率（当前 0.5 已经是较好平衡点，继续降低会出现明显 AO 颗粒感）

---

## 执行顺序建议

1. **优化项 1**（阴影脏检查阈值化）— 预期收益最大，改动量最小，先做。
2. **优化项 2**（DOM 缓存）— 顺手改，几乎零成本。
3. **优化项 3**（antialias 验证）— A/B 测试后再决定是否保留。

每完成一项，用 Performance 面板录制 5~10 秒漂移动画，记录平均 FPS 和 `WebGLShadowMap.render` / `WebGLRenderer.render` 耗时占比，作为对照基线。
