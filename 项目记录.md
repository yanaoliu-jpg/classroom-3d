# 空教室渲染项目 · 工作记录

> 主文件：`classroom_v4_3.html`（单文件 Three.js r164 渲染，直接浏览器打开）
> 最近更新：2026-09-24

---

## 1. 快速上手

- **打开**：浏览器直接打开 `classroom_v4_3.html`。
- **面板**：右下角控制光照（窗光/太阳、吊顶灯、走廊灯），顶部切换时间预设（清晨/正午/傍晚/夜/自定义）。
- **漫游**：拖动旋转视角；滚轮缩放；**WASD** 平移；**Q 下降 / E 上升**。

### 调试 / 报错热键（DBG 工具，平时静默）
| 键 | 作用 |
|---|---|
| `R` | **报告模式**：点画面异常处 → 自动生成"画面异常报告"(含机位+灯光状态的复现链接) + 复制到剪贴板 + 存带红圈截图。**把报告粘给 AI + 补一句症状即可** |
| `I` | 逐网格随机上色（判断某块是 mesh 还是光照——发光贴片会保持原色） |
| `U` | UV 棋盘 ｜ `O` 线框 ｜ `H` HUD ｜ `P` 打印机位 ｜ `0` 还原 |
| `g` | 体积阳光光束(god-ray)强度，默认 0 关闭 |
| `-` / `=` | 实时调地面反照率（明暗微调） |

### 常用热键
| 键 | 作用 |
|---|---|
| `1`–`4` | 时间预设：清晨 / 正午 / 傍晚 / 深夜 |
| `5` `6` `7` `9` `0` | 固定机位：全景 / 教师视角 / 后→前广角 / 侧窗全景 / 教室外观 |
| `T` | 开关时间轴（一天 05:30→22:30 连续推移） |
| `Tab` | 收起 / 展开底部控制面板 |
| `W A S D` / `Q E` | 漫游平移 / 升降 |

- **URL hash 定位**：`classroom_v4_3.html#preset=morning&theta=…&phi=…&camR=…&panX/Z/Y=…&mInt=…&ceilInt=…` —— 一个网址直达任意机位+灯光状态（报告里的复现链接就是这个）。

---

## 2. 无头渲染（AI 调试用）

系统 Chrome + swiftshader/metal 截图。**注意 swiftshader 比真机暗 ~1.6–2.4×**，亮度/过曝类问题需用曝光代理复现，或以真机 `R` 报告为准。
```
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --use-angle=metal \
  --enable-webgl --ignore-gpu-blocklist --hide-scrollbars --window-size=1600,850 \
  --user-data-dir=/tmp/cdirX --screenshot=out.png "file://<路径>/classroom_v4_3.html#<hash>"
```
- 路径含中文"修复"→URL 编码为 `%E4%BF%AE%E5%A4%8D`。
- 不要用 `--virtual-time-budget`（会挂死）。每次给独立 `--user-data-dir`，冷启动约 30s。

---

## 3. 已完成的修复与改动（时间倒序）

### 2026-09-24 · 帧率 21→90+ FPS、时间轴复活、UI 修整

**帧率（详见 `性能优化计划.md`）**
实测定位：帧时间的 ~93% 花在"每个 fragment 遍历场景里全部 39 盏灯"的片元着色上，
与网格数/draw call/阴影/后处理几乎无关；而且代价是超线性的（灯多到一定程度寄存器溢出、GPU 占用率崩塌）。
对策全部是**真的减少每帧计算量**，没有用任何"运动中降分辨率/模糊"的补帧手段：
- SpotLight 改为只算漫反射（10 盏全是 `castShadow=false` 的间接光近似，本就不该有镜面高光）
- 走廊 8 盏吸顶灯 / 教室 7 盏反弹补光，按相机在教室还是在走廊**整组开关**（`LAYER_CORRIDOR` / `LAYER_ROOMFILL`）
- 2 盏门玻璃 RectAreaLight（最贵的灯型）换成漫反射聚光；3 盏地面反弹合并为 1 盏；删掉恒为 0 的 `corridorLight`
- 灯数变化会触发整套着色器重编（首次 0.4~1.9s），所以在开场封面期间预热两种组合（`warmUpShaderVariants`）
结果：**各预设 × 各机位 90–161 FPS**（原 21–22），切换房间时一帧内完成、无卡顿。
顺带把画幅 `FRAME_FILL` 0.88→0.94（画面更大），仍稳在 90 FPS 以上。

**删除「动态分辨率 · 转动时降」开关** —— 它靠转动时降分辨率换帧率，代价是画面糊一下再变清晰，
属于用模糊补帧。现在分辨率全程恒定，转动中不掉画质。

**修掉的缺陷**
- **时间轴 UI 整块缺失**：CSS、全部逻辑（`toggleDrift`/`togglePlay`/`setDriftSpeed`/`onTimelineScrub`）、
  渲染循环里那整段 `if(driftOn){…}`（日夜光照、自动开关灯、时钟）都在，唯独没有任何标签能把它打开
  → 日光推移这个主功能无法触达，且 `toggleDrift()` 一旦被调用就会抛 `TypeError(null)`。已按原 CSS 结构补回标签。
- **时间轴滑块完全看不见**：`#timeSlider` 没有类名，而全局 `input[type=range]` 关了 `-webkit-appearance`
  且只给 `.g/.b/.r` 的 thumb 上色 → 轨道和滑块都是透明的。现在给它一条"一天的颜色"渐变轨道。
- **时间轴压在控制面板上**：原 `bottom:60px`，而面板高约 129px。改为按 `--panel-h` 上浮，
  并在打开时从可用区扣掉一条 `TIMELINE_STRIP`，画面自动上移让位。帧率读数/罗盘同样改为跟随面板高度。
- **离屏渲染目标是小数尺寸**（1576.75×887.25）→ 整画面轻微发虚、转动时边缘抖动。已统一用整数设备像素。
- **GTAO 分辨率随清晰度档位漂移**（档位越高 AO 相对越糊，正好反了）。已改为跟 `curRes` 走。
- 开时间轴后点预设看上去没反应（漂移每帧把滑块写回）→ 现在点预设即退出时间轴。
- 删掉 `setAspect`（无调用点，且会连天气/背景/清晰度/机位按钮一起清掉 `.on` 高亮）、
  `pixelRatioCap`/`SUPERSAMPLE`（无调用点）、`renderer.useLegacyLights`（r164 已废弃，只会刷警告）。
- 主界面补上 `:focus-visible` 焦点描边（此前只有封面两个按钮有，Tab 走一遍看不出焦点在哪）。
- 底部操作提示改为首次交互后（或 12 秒）淡出，不再一直挂在画面下沿。

**新增**：`T` 键开关时间轴；顶栏「⏱ 时间轴」按钮；三个顶栏按钮加 `title` 说明。

### 光照 / 阴影
- **阴影漏光根治（重要）**：方向光 position 离墙仅 ~2m 太近 → 阴影相机深度精度崩溃 → 阳光漏穿实心墙。修法：`SUN_DIST=4` 把光源沿同方向外推 4 倍（方向/光照不变，只改阴影相机位置）+ shadow far 55→140。静态与时间轴两条路径都改。所有太阳角度漏光消失。
- 封各墙漏光：后/前/右墙加 `castShadow`；左墙外遮光板 z 范围延伸过墙角 + 女儿墙。
- 太阳提亮（晨 mInt 4.0 / 午 4.1 / 暮 3.5）；吊顶灯默认亮度 1.8。
- 地面"死白"根因排查与修复（见记忆库，共 5 个独立来源：winPatch / sunBeams / 走廊灯 winFloor+winSpot / 光照地面+高光叠层 / 走廊几何尺寸写反）。

### 窗户
- 左墙改为**真窗洞**（ShapeGeometry 挖洞，原来是实墙+假窗贴面）。
- 去掉窗格栏杆、窗帘、玻璃 tint；窗户加大（更宽更高、窗台放低）。
- 窗外无贴图/风景/天空 —— 纯通透开口（窗外景已移除）。

### 天花板 / 墙 / 家具
- 吊顶灯 2 列→**3 列**；每组灯具正下方一盏点光源（3×4 对齐，用 `ceilLevel` 统一驱动）。
- 天花板底板 + 后墙材质**统一为左墙材质**。
- 黑板上方加**书写灯管**（随吊顶灯开关）。
- 删除"班级格言"横幅，改为**五星红旗**（书写灯上方）。

### 交互 / 功能
- **报告工具**（`R` 键）：定位物体 + 复现链接（含灯光状态）+ 剪贴板报告 + 红圈截图。
- Q/E 升降调换；移除空中尘埃光粒。
- **删除窗帘/关窗遮光功能**（含时间轴午休自动拉帘）。
- 大而远的**太阳球**（视觉，透过窗可见；照明仍由方向光）。

---

## 4. 已知情况 / 注意

- **窗外背景**：现为一张等距柱状全景（蓝天+云）作 `scene.background`，设置面板可切「蓝天背景 / 纯黑背景」；只影响背景，不改室内光照。
- **改灯的数量时要当心**：three.js 按灯数缓存着色器程序，数量一变就要重编整套（首次 0.4~1.9s 卡顿）。若新增按条件开关的灯组，务必同时在 `warmUpShaderVariants` 里把新的组合也预热一遍。
- 无头渲染偏暗，亮度/过曝/镜面高光类问题以真机 `R` 报告为准。
- 判断漏光：光斑若在**窗户正对区**多为正常窗光；出现在**背光角落/墙根**才可能是漏光。

---

## 5. Git

- 主分支 `main`，改动走 `fix:/feat:` 分支 + `--no-ff` merge（提交信息以 `Co-Authored-By: Claude` 结尾）。
- 远端：`origin` → https://github.com/yanaoliu-jpg/classroom-3d.git（`main` 已跟踪 `origin/main`）。
- 最近关键提交：
  - `403a57b` 阴影漏光修复(SUN_DIST) + 窗户/太阳/材质大改
  - `83d13ed` 更亮的灯 + 清透大窗(无栏杆/窗帘) + 吊顶灯对齐
  - `c986090` 地面死白(5 根因) + 精准报错工具

---

## 6. 相关文档

- 设计 spec：`docs/superpowers/specs/2026-06-30-precise-error-report-workflow-design.md`
- 前后对比图：`docs/superpowers/compare/`（floor_deadwhite_*、corridor_floor_geometry_fix 等）
- AI 记忆库（跨会话）：`~/.claude/projects/-Users-liuyanao-Desktop---/memory/`（含漏光根因、死白修复、报错流程等）
